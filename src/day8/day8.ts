import { GetFileData } from '../util';

const inputData = await GetFileData();

const lines = inputData.split('\n');
console.time(`Process Time`);

console.log(`Part 1: n/a`);
console.log(`Part 2: n/a`);

console.timeEnd(`Process Time`);
